(ns ppp.http-server.core-test
  (:require [clojure.test :as test :refer :all]
            [ppp.http-server.core :as core]))

(deftest dummy-test
  (is (= 1 1)))

(deftest dummy-test-2
  (is (= 1 2)))
